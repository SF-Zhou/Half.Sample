#ifndef SAMPLER_PARA_HPP
#define SAMPLER_PARA_HPP

namespace Sampler {

struct SamplerConfig {
    int sampling_frequency;
    int sampling_length;

    int number_of_waveforms;
};

} // namespace Sampler

#endif
